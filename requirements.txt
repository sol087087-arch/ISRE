# ISRE v1 dependencies
# Pinned to floor versions matching the dev environment (Python 3.12).

torch>=2.4
tqdm>=4.66
jinja2>=3.1

# Optional: SVG AST rendering in trajectory_viewer.
# Requires the Graphviz system binary (https://graphviz.org/download/) on PATH.
# Without it the viewer falls back to ASCII trees.
graphviz>=0.20
